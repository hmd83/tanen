# Production overlay — no logging, size-optimized
CONFIG_LOG=n
CONFIG_CONSOLE=n
CONFIG_UART_CONSOLE=n
CONFIG_SIZE_OPTIMIZATIONS=y
# Override measurement interval for production (15 min)
CONFIG_TANENBASE_DEFAULT_MS_INTERVAL=900